<Window x:Class="ex_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ex_1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <TextBox DockPanel.Dock="Bottom" Text="{Binding TextBoxString}" Height="30"/>
        <Button DockPanel.Dock="Bottom" Command="{Binding Path=SearchButtonClick}" Height="30" Content="Search" FontSize="20"/>
        <ListView ItemsSource="{Binding CurrentFiles}"/>
    </DockPanel>
</Window>
